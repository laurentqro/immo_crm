<%= content_for :title, "Property Management - Step 4 of 7" %>

<div class="container mx-auto px-4 py-8">
  <%= render "submission_steps/progress", current_step: 4 %>

  <div class="max-w-4xl mx-auto">
    <h1 class="text-2xl font-bold text-gray-900 dark:text-white mb-2">Property Management Statistics</h1>
    <p class="text-gray-600 dark:text-gray-300 mb-6">Review statistics for managed rental properties (gestion locative) for <%= @submission.year %>.</p>

    <%# Lock status banner %>
    <% if @submission.locked? && @submission.locked_by?(Current.user) %>
      <div class="bg-blue-50 dark:bg-blue-900/20 border border-blue-200 dark:border-blue-800 rounded-md p-4 mb-6">
        <div class="flex">
          <div class="flex-shrink-0">
            <svg class="h-5 w-5 text-blue-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
            </svg>
          </div>
          <div class="ml-3">
            <p class="text-sm text-blue-700 dark:text-blue-300">
              You have locked this submission for editing.
              <%= button_to "Unlock", unlock_submission_submission_steps_path(@submission, step: 4), method: :post, class: "underline ml-2", data: { turbo_confirm: "Unlock this submission?" } %>
            </p>
          </div>
        </div>
      </div>
    <% elsif @submission.locked? %>
      <div class="bg-yellow-50 dark:bg-yellow-900/20 border border-yellow-200 dark:border-yellow-800 rounded-md p-4 mb-6">
        <div class="flex">
          <div class="flex-shrink-0">
            <svg class="h-5 w-5 text-yellow-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
            </svg>
          </div>
          <div class="ml-3">
            <p class="text-sm text-yellow-700 dark:text-yellow-300">
              This submission is being edited by another user.
            </p>
          </div>
        </div>
      </div>
    <% end %>

    <%# Property Statistics Cards %>
    <div class="bg-gray-50 dark:bg-gray-800/50 rounded-lg p-4 mb-6">
      <div class="flex items-center justify-between mb-4">
        <div>
          <h3 class="text-lg font-semibold text-gray-900 dark:text-white">Managed Properties Overview</h3>
          <p class="mt-1 text-sm text-gray-500 dark:text-gray-400">Statistics from your managed rental portfolio</p>
        </div>
        <% if @yoy_comparison.values.any? { |c| c[:significant] } %>
          <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-amber-100 text-amber-800 dark:bg-amber-900/30 dark:text-amber-300">
            Review Required
          </span>
        <% end %>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        <%# Active Properties %>
        <div class="bg-white dark:bg-gray-800 rounded-lg p-4 border border-gray-200 dark:border-gray-700">
          <div class="flex justify-between items-start">
            <div>
              <p class="text-sm text-gray-500 dark:text-gray-400">Active Properties</p>
              <p class="text-2xl font-semibold text-gray-900 dark:text-white mt-1"><%= @property_stats["aACTIVEPS"] || 0 %></p>
              <p class="text-xs text-gray-400 font-mono mt-1">aACTIVEPS</p>
            </div>
            <% yoy = @yoy_comparison["aACTIVEPS"] %>
            <% if yoy && yoy[:change_percent] %>
              <span class="inline-flex items-center px-2 py-1 rounded text-xs font-medium <%= yoy[:change_percent] >= 0 ? 'bg-green-100 text-green-800 dark:bg-green-900/30 dark:text-green-300' : 'bg-red-100 text-red-800 dark:bg-red-900/30 dark:text-red-300' %>">
                <%= yoy[:change_percent] >= 0 ? '+' : '' %><%= yoy[:change_percent] %>%
              </span>
            <% end %>
          </div>
        </div>

        <%# Total Tenants %>
        <div class="bg-white dark:bg-gray-800 rounded-lg p-4 border border-gray-200 dark:border-gray-700">
          <div class="flex justify-between items-start">
            <div>
              <p class="text-sm text-gray-500 dark:text-gray-400">Total Tenants</p>
              <p class="text-2xl font-semibold text-gray-900 dark:text-white mt-1"><%= @property_stats["a1802TOLA"] || 0 %></p>
              <p class="text-xs text-gray-400 font-mono mt-1">a1802TOLA</p>
            </div>
            <% yoy = @yoy_comparison["a1802TOLA"] %>
            <% if yoy && yoy[:change_percent] %>
              <span class="inline-flex items-center px-2 py-1 rounded text-xs font-medium <%= yoy[:change_percent] >= 0 ? 'bg-green-100 text-green-800 dark:bg-green-900/30 dark:text-green-300' : 'bg-red-100 text-red-800 dark:bg-red-900/30 dark:text-red-300' %>">
                <%= yoy[:change_percent] >= 0 ? '+' : '' %><%= yoy[:change_percent] %>%
              </span>
            <% end %>
          </div>
        </div>

        <%# Natural Person Tenants %>
        <div class="bg-white dark:bg-gray-800 rounded-lg p-4 border border-gray-200 dark:border-gray-700">
          <div class="flex justify-between items-start">
            <div>
              <p class="text-sm text-gray-500 dark:text-gray-400">Natural Person Tenants</p>
              <p class="text-2xl font-semibold text-gray-900 dark:text-white mt-1"><%= @property_stats["a1802TOLA_NP"] || 0 %></p>
              <p class="text-xs text-gray-400 font-mono mt-1">a1802TOLA_NP</p>
            </div>
            <% yoy = @yoy_comparison["a1802TOLA_NP"] %>
            <% if yoy && yoy[:change_percent] %>
              <span class="inline-flex items-center px-2 py-1 rounded text-xs font-medium <%= yoy[:change_percent] >= 0 ? 'bg-green-100 text-green-800 dark:bg-green-900/30 dark:text-green-300' : 'bg-red-100 text-red-800 dark:bg-red-900/30 dark:text-red-300' %>">
                <%= yoy[:change_percent] >= 0 ? '+' : '' %><%= yoy[:change_percent] %>%
              </span>
            <% end %>
          </div>
        </div>

        <%# Legal Entity Tenants %>
        <div class="bg-white dark:bg-gray-800 rounded-lg p-4 border border-gray-200 dark:border-gray-700">
          <div class="flex justify-between items-start">
            <div>
              <p class="text-sm text-gray-500 dark:text-gray-400">Legal Entity Tenants</p>
              <p class="text-2xl font-semibold text-gray-900 dark:text-white mt-1"><%= @property_stats["a1802TOLA_LE"] || 0 %></p>
              <p class="text-xs text-gray-400 font-mono mt-1">a1802TOLA_LE</p>
            </div>
            <% yoy = @yoy_comparison["a1802TOLA_LE"] %>
            <% if yoy && yoy[:change_percent] %>
              <span class="inline-flex items-center px-2 py-1 rounded text-xs font-medium <%= yoy[:change_percent] >= 0 ? 'bg-green-100 text-green-800 dark:bg-green-900/30 dark:text-green-300' : 'bg-red-100 text-red-800 dark:bg-red-900/30 dark:text-red-300' %>">
                <%= yoy[:change_percent] >= 0 ? '+' : '' %><%= yoy[:change_percent] %>%
              </span>
            <% end %>
          </div>
        </div>

        <%# PEP Tenants %>
        <div class="bg-white dark:bg-gray-800 rounded-lg p-4 border border-gray-200 dark:border-gray-700 <%= @yoy_comparison["a1802PEP"]&.dig(:significant) ? 'ring-2 ring-amber-400' : '' %>">
          <div class="flex justify-between items-start">
            <div>
              <p class="text-sm text-gray-500 dark:text-gray-400">PEP Tenants</p>
              <p class="text-2xl font-semibold text-gray-900 dark:text-white mt-1"><%= @property_stats["a1802PEP"] || 0 %></p>
              <p class="text-xs text-gray-400 font-mono mt-1">a1802PEP</p>
            </div>
            <% yoy = @yoy_comparison["a1802PEP"] %>
            <% if yoy && yoy[:change_percent] %>
              <span class="inline-flex items-center px-2 py-1 rounded text-xs font-medium <%= yoy[:significant] ? 'bg-amber-100 text-amber-800 dark:bg-amber-900/30 dark:text-amber-300' : (yoy[:change_percent] >= 0 ? 'bg-green-100 text-green-800 dark:bg-green-900/30 dark:text-green-300' : 'bg-red-100 text-red-800 dark:bg-red-900/30 dark:text-red-300') %>">
                <%= yoy[:change_percent] >= 0 ? '+' : '' %><%= yoy[:change_percent] %>%
                <% if yoy[:significant] %>
                  <svg class="ml-1 h-3 w-3" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd" />
                  </svg>
                <% end %>
              </span>
            <% end %>
          </div>
        </div>
      </div>
    </div>

    <%# Navigation %>
    <%= form_with model: @submission, url: submission_submission_step_path(@submission, step: 4), method: :patch, local: true do |f| %>
      <div class="flex justify-between">
        <%= f.submit "Back", name: "commit", value: "back", class: "btn btn-secondary" %>
        <%= f.submit "Continue", name: "commit", value: "continue", class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>
</div>
